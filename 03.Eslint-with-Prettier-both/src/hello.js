export const sayHi = hi => {
  return `Hello ${hi}`;
};

export function greet(msg) {
  return `Hello ${msg}`;
}

let name = "error"; // 'name' is assigned a value but never used.

export const msg = "Hello World";
