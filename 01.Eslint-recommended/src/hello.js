const msg = 'Hello World';

const sayHi =(hi)=>{
  return `Hello ${hi}`
}

const msg = "Hello World";

function greet  (msg) {
  return `Hello ${msg}`
}

let name  = "error"; // 'name' is assigned a value but never used.

export default msg

export {greet}
